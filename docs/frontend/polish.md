# План доопрацювання React-адмінки

Цей документ фіксує погоджені інкременти для підготовки адмінки до презентації. Після кожного кроку виконується візуальна перевірка та створюється окремий коміт.

## 1. Брендинг середовищ

- Додати змінну `VITE_APP_VARIANT`.
- Відображати `Orest Web` для `VITE_APP_VARIANT=web` та `Orest Local` для `VITE_APP_VARIANT=local`.
- Оновити назву на екрані входу та в лівій панелі.

Візуальна перевірка: запустити застосунок із кожним значенням змінної та перевірити обидва місця відображення.

Коміт: `feat(ui): add environment-specific Orest branding`

## 2. Спільна навігація

- Прибрати індикатор готовності з верхньої панелі.
- Перенести кнопку «Вийти» в нижню частину лівої панелі.

Візуальна перевірка: перевірити три вкладки, вихід із сесії та вигляд на вузькому екрані.

Коміт: `feat(ui): move logout to sidebar`

## 3. Базове покращення AI-вкладки

- Відображати в полі «Місяць для підказок» тільки місяці, у яких є транзакції.
- Зробити кнопку «Прикріпити чек» блідо-зеленою.
- Після вибору чека заповнювати поле повідомлення текстом «Проаналізуй прикріплений чек і підготуй чернетку витрати.», не перезаписуючи вже введений текст.
- Деталізувати статуси недоступних чернеток: скасована, термін дії минув, помилка обробки.

Візуальна перевірка: вручну перевірити кожен сценарій на вкладці AI-помічника.

Коміт: `feat(ai-ui): improve receipt and draft workflow`

## 4. Редагована чернетка та проблемні позиції чека

- Показувати одну «Дату виконання операцій» для всіх позицій у верхній частині чернетки.
- Відображати список у форматі «п/н | категорія товару | сума» без поля типу транзакції.
- Залишати проблемні позиції на їхньому місці в загальному списку та підсвічувати їх.
- Давати змогу заповнювати невизначені дату, категорію та суму безпосередньо в рядку чернетки.
- Для категорії використовувати поле з підказками вже наявних категорій і дозволяти ввести нову — як у вкладці «Редагування»; окремий довідник і API не створювати.
- Зберігати зміни чернетки серверно та розблоковувати «Підтвердити» лише після валідації всіх полів.
- Додати кнопку «Показати чек» і тимчасову праву панель захищеного перегляду PDF або зображення; закривати її вручну, після підтвердження або скасування чернетки.

Візуальна перевірка: прикріпити чек із проблемними позиціями, заповнити поля в рядках, перевірити розблокування підтвердження, перегляд чека та коректне створення витрат.

Коміт: `feat(ai-receipts): add editable receipt draft workflow`

## 5. Новий діалог

- Реалізувати кнопку «Очистити чат» як створення нового порожнього діалогу.
- Зберігати попередню історію в БД; не видаляти її.
- Додати підтвердження дії та серверний тест.

Візуальна перевірка: надіслати повідомлення, почати новий діалог, переконатися, що інтерфейс порожній, перезавантажити сторінку та перевірити, що відкривається новий діалог.

Коміт: `feat(ai-chat): start a new conversation`

## 6. Керовані AI-підказки

- Додати таблицю та API для спільних AI-підказок.
- Замінити набір кнопок підказок на dropdown.
- Для користувача з роллю `admin` додати створення і видалення підказок.

Візуальна перевірка: перевірити список, вибір підказки, додавання та видалення адміністратором, а також недоступність редагування для користувача без прав.

Коміт: `feat(ai-chat): manage shared prompt suggestions`

## 7. Фінальна перевірка

- Перевірити адаптивність і регресії на всіх трьох вкладках.
- Зібрати React-проєкт і запустити релевантні серверні тести.

Коміт (лише якщо з'являться виправлення): `fix(ui): polish admin dashboard presentation`

## Результат виконання гілки `frontend/polish`

Усі сім інкрементів плану виконано. Фінальна перевірка не потребувала окремих виправлень.

| Пункт | Результат | Коміт |
| --- | --- | --- |
| 1 | Додано брендинг `Orest Web` / `Orest Local` через `VITE_APP_VARIANT`. | `7f47757` |
| 2 | Індикацію готовності прибрано, вихід перенесено до нижньої частини лівої панелі. | `18843b5` |
| 3 | Оновлено AI-вкладку: доступні місяці, прикріплення чека та стани чернеток. | `ca22ae4` |
| 4 | Додано редаговані чернетки чеків, валідацію та захищений перегляд вкладення. | `82eb540` |
| 5 | «Очистити чат» створює новий порожній діалог і не видаляє попередню історію. | `6512893` |
| 6 | Додано керовані підказки, API, dropdown і доступні для адміністратора створення та видалення. | `5bd63a0`, `cc56959`, `4752b09`, `a6f49d8` |
| 7 | Перевірено адаптивність, серверні тести та production-збірку. | — |

### Міграції

Для керованих підказок додано такі Alembic-міграції:

- `20260809_05_add_ai_prompt_suggestions` — створює таблицю `ai_prompt_suggestions`.
- `20260809_06_seed_ai_prompt_suggestions` — додає п’ять попередніх підказок із шаблоном `{{month}}`; безпечно пропускає підказки, що вже існують.

Застосування міграцій:

```powershell
.\.venv\Scripts\python.exe -m alembic upgrade head
```

### Перевірки

- Повний набір серверних тестів: 38 успішних.
- Production-збірка React через `npm run build`: успішно.
- Сканування відстежуваних Git файлів на секрети: реальних секретів не знайдено; єдиний збіг — тестове значення `client-secret` у тесті Google Drive.
